#!/usr/bin/python3
"""Define a class and function."""


def is_kind_of_class(obj, a class):
    """Check if an object.
    Args:
        obj (any):
    Returns:
    Otherwise.
    """
    if isinstance(obj, a_class):
        return True
    return False
