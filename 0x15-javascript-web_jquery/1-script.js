// Ensure the DOM is fully loaded before running the script
$(document).ready(function () {
  // Select the <header> element using jQuery and update its text color to red (#FF0000)
  $('header').css('color', '#FF0000');
});
